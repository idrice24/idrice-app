// here is the data model for contact
// now we import the model file in the components we wish to use in it...

export interface Contact {
	
		 id?: number;
	     msg?: string;
		 uname?: string;
		 email?: string;
		 SubmitDate?: Date;

}